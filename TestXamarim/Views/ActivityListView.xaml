<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TestXamarim.Views.ActivityListView" Title="Bravi Timeline">
	<ContentPage.Content>
		<StackLayout>
			<StackLayout>
				<ListView x:Name="myListView" ItemsSource="{Binding ActivityList}" SeparatorVisibility="Default" RowHeight="100">
				    <ListView.ItemTemplate>
				      <DataTemplate>
				      	<ViewCell>

				      		<ViewCell.ContextActions>
				      			<MenuItem Text="Delete" Clicked="OnDelete" CommandParameter="{Binding .}" />
				      		</ViewCell.ContextActions>

							<StackLayout Orientation="Vertical" Padding="10">
								<StackLayout>
									<StackLayout Orientation="Horizontal" HorizontalOptions="Fill" BackgroundColor="#5A595B" Padding="5">
										<Label Text="From Mauricio Minella" HorizontalOptions="Start" TextColor="White" FontSize="13" />
										<Label Text="{Binding Date, StringFormat='{0:dd/MM/yyyy}'}"  HorizontalOptions="End" TextColor="White" FontSize="13" />
									</StackLayout>
									<Label Text="{Binding Description}" />
								</StackLayout>
			
							</StackLayout>

				      	</ViewCell>
				      </DataTemplate>
				    </ListView.ItemTemplate>
				  </ListView>
				  <Button Text="Add New Activity" Command="{Binding AddNewActivityCommand}" />
			</StackLayout>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
